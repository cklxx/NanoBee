# ========================================
# 后端配置 (Backend Configuration)
# ========================================

# 工作空间根目录 - 存储提示词笔记和生成的文件
# Workspaces root directory for prompt notebooks and generated artifacts
NANOBEE_WORKSPACES_ROOT=./workspaces

# CORS 允许的源 - 可以使用逗号分隔或 JSON 数组格式
# Allowed CORS origins (comma-separated or JSON array format)
NANOBEE_CORS_ORIGINS=["http://localhost:3000","http://localhost:3001","http://127.0.0.1:3000","http://127.0.0.1:3001"]

# ----------------------------------------
# 文本生成模型配置 (Text Model Configuration)
# ----------------------------------------

# 默认文本模型名称 - 用于 PPT 内容生成
# Default text model for PPT content generation
NANOBEE_DEFAULT_TEXT_MODEL=doubao-seed-1-6-251015

# Anthropic 兼容的基础 URL（可指向本地代理）
# Anthropic-compatible base URL (can point to local proxy)
ANTHROPIC_BASE_URL=http://localhost:8000/proxy

# Anthropic API Key（或代理转发时使用的文本模型 API Key）
# Anthropic API key or proxy-forwarded text model key
ANTHROPIC_API_KEY=

# 文本模型 API 基础 URL
# Text model API base URL
NANOBEE_DEFAULT_TEXT_BASE_URL=https://ark.cn-beijing.volces.com/api/v3

# 文本模型 API 密钥 - ⚠️ 必填，用于调用文本生成服务
# Text model API key - REQUIRED for text generation service
NANOBEE_TEXT_API_KEY=

# ----------------------------------------
# Claude Code Proxy / OpenAI 兼容配置
# ----------------------------------------

# OpenAI 兼容 API 基础 URL（默认官方，也可指向 DeepSeek/Ollama 等）
OPENAI_BASE_URL=https://api.openai.com/v1

# OpenAI 兼容 API Key
OPENAI_API_KEY=

# 代理模型映射（根据 Haiku/Sonnet/Opus 自动映射）
OPENAI_BIG_MODEL=gpt-4o
OPENAI_MIDDLE_MODEL=gpt-4o
OPENAI_SMALL_MODEL=gpt-4o-mini

# ----------------------------------------
# 图像生成模型配置 (Image Model Configuration)
# ----------------------------------------

# 默认图像模型名称 - 用于 PPT 图片生成
# Default image model for PPT image generation
NANOBEE_DEFAULT_IMAGE_MODEL=doubao-seedream-4-5-251128

# 图像模型 API 基础 URL
# Image model API base URL
NANOBEE_DEFAULT_IMAGE_BASE_URL=https://ark.cn-beijing.volces.com/api/v3

# 图像模型 API 密钥 - ⚠️ 必填，用于调用图像生成服务
# Image model API key - REQUIRED for image generation service
NANOBEE_IMAGE_API_KEY=

# 是否允许 AI 生成图像添加水印 (true/false 或 1/0)
# Whether to allow AI watermarks on generated images (true/false or 1/0)
NANOBEE_ALLOW_IMAGE_WATERMARK=false

# ========================================
# 前端配置 (Frontend Configuration)
# ========================================

# 后端 API 地址 - 前端用于调用后端服务
# Backend API base URL for frontend to call backend services
# 开发环境: http://localhost:8000
# Docker 部署: /api (通过 Nginx 反向代理)
NEXT_PUBLIC_API_BASE=http://localhost:8000

# ========================================
# Docker 部署配置 (Docker Deployment)
# ========================================

# Docker 部署说明:
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 配置上方必需的 API 密钥 (NANOBEE_TEXT_API_KEY, NANOBEE_IMAGE_API_KEY)
# 3. 运行一键启动脚本: ./docker-start.sh
# 4. 访问 http://localhost (或服务器 IP) 即可使用
#
# 腾讯云轻量服务器部署:
# - 脚本已配置国内镜像源加速（pip: 清华源, npm: 腾讯云源）
# - 确保服务器安全组开放 80 端口
# - 首次构建可能需要 5-10 分钟，请耐心等待
