# ========================================
# 前端配置 (Frontend Configuration)
# ========================================

# 后端 API 地址 - 前端用于调用后端服务
# Backend API base URL for frontend to call backend services
# 开发环境使用 localhost，生产环境请替换为实际的后端地址
# Use localhost for development, replace with actual backend URL in production
NEXT_PUBLIC_API_BASE=http://localhost:8000
